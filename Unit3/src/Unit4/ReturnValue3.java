/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Unit4;

/**
 *
 * @author s199741231
 */
public class ReturnValue3 {

    /*
     public static double f (double x)
     {
     double y;
     if (x < 0)
     {
     y = x * x;
     }
     else
     {
     y = x * x * x;
     }
     return y;
     }
    
     a) System.out.println(f(-7));
     b) double x = f(-7);
     c) double x = System.out.println(f(-7)); <-- This is invalid because you can't define a variable as something to be printed
     d) double x = -7; f(x);
     */
}
